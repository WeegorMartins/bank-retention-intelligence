name: "bank_retention_intelligence"
version: "1.0.0"
config-version: 2
profile: "bank_retention_intelligence"
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]
models:
  bank_retention_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table

